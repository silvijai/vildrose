# VM

This guide will explain loading programs, stepping execution, and tracing behavior. Later on will also try to connect this to a display crate, and try to make some GPU implementation. In ideal world, I'd get to have a system with a hybrid CPU that has ternary and binary cores, that also interacts with a GPU that is the same way.
